filename = "push_endp_redshift.lua"
message_matcher =  'Logger =~ "^Autoendpoint\-" && Fields[remote-ip] != NIL'
async_buffer_size = 1000
format = "redshift"
memory_limit = 1024 * 1024 * 128

db_config = {
    name = "pushderived",
    user = "push",
    _password = "<!!!!!SECRET!!!!!>",
    host = "PROD.PUSH.REDSHIFT.HOST",
    port = 5439
}

buffer_path = "/home/ec2-user/push_load/hindsight/buffers"

